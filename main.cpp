#include <iostream>
#include "Pilha.h"
using namespace std;

int main()
{
   /* Pilha p(32);
    unsigned n;
    cout << "Número positivo? ";
    cin >> n;
    do {
        p.insere(n%2);
        n /= 2;
    } while( n!=0 );
    cout << "Binário: ";
    while( !p.vazia() )
        cout << p.remove();
    cout << endl;
    */

    return 0;
}
